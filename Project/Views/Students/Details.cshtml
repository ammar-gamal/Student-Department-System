@model Student
@{
    ViewBag.Title = "Student Details";
    Department dept = Model.Department;

}
    <h2 class="alert alert-danger">
    <b>Name:</b>  @Model.Name         <br/>
    <b>Password:</b> @Model.Password  <br/>
    <b>Email:</b> @Model.Email        <br/>
    <b>Gender:</b> @Model.Gender      <br/>
    <b>Age:</b> @Model.Age            <br/>
    <b>Address:</b> @Model.Address    <br/>
    <b>BirthDate:</b> @(Model.DoB.ToString("yyyy-MM-dd"))  <br/>
    <b>DepartmentName:</b> @((dept is not null)?dept.deptName:"None")     <br />
    <b>DepartmentID:</b> @((dept is not null) ? dept.deptId : "None")          <br />
     </h2>
   
<a asp-controller="students" asp-action="index" class="button-link CenterBack">Back</a>